apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatws.fullname" . }}
  labels:
    {{- include "chatws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatws.selectorLabels" . | nindent 4 }}
